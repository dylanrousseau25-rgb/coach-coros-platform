(() => {
  const KEY='coach-plan-continuity-v1';
  const originalFetch=window.fetch.bind(window);
  const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'null')||{deletedObjectiveIds:[],objectives:[],plans:[],planVersions:[],planProposals:[],savedAt:null};}catch{return{deletedObjectiveIds:[],objectives:[],plans:[],planVersions:[],planProposals:[],savedAt:null};}};
  const write=value=>{try{localStorage.setItem(KEY,JSON.stringify({...value,savedAt:new Date().toISOString()}));}catch(error){console.warn('Plan continuity write',error);}};
  function overlay(data){const local=read();if(!local.savedAt)return data;const deleted=new Set(local.deletedObjectiveIds||[]),objectives=(data.objectives||[]).filter(o=>!deleted.has(o.id)),plans=(data.plans||[]).filter(p=>!deleted.has(p.objectiveId));for(const o of local.objectives||[]){if(deleted.has(o.id))continue;const i=objectives.findIndex(x=>x.id===o.id);if(i>=0)objectives[i]=o;else objectives.unshift(o);}for(const p of local.plans||[]){if(deleted.has(p.objectiveId))continue;const i=plans.findIndex(x=>x.id===p.id);if(i>=0)plans[i]=p;else plans.unshift(p);}const activeObjective=objectives.find(o=>o.status==='active')||null,activePlan=activeObjective?plans.find(p=>p.id===activeObjective.planId)||null:null,today=data.meta?.today||new Date().toISOString().slice(0,10);return{...data,objectives,plans,planVersions:local.planVersions?.length?local.planVersions:(data.planVersions||[]),planProposals:local.planProposals?.length?local.planProposals:(data.planProposals||[]),activeObjective,activePlan,todaySession:activePlan?.sessions?.find(s=>s.date===today)||null,meta:{...(data.meta||{}),deviceContinuity:true}};}
  async function capture(data=null){try{if(!data){const r=await originalFetch('/api/dashboard',{cache:'no-store'});if(!r.ok)return;data=await r.json();}const prev=read(),deleted=new Set(prev.deletedObjectiveIds||[]);write({deletedObjectiveIds:[...deleted],objectives:(data.objectives||[]).filter(o=>!deleted.has(o.id)),plans:(data.plans||[]).filter(p=>!deleted.has(p.objectiveId)),planVersions:data.planVersions||prev.planVersions||[],planProposals:(data.planProposals||[]).filter(p=>p.status!=='proposed'),savedAt:new Date().toISOString()});}catch(error){console.warn('Plan continuity capture',error);}}
  function markDeleted(objectiveId){const local=read(),deleted=new Set(local.deletedObjectiveIds||[]);deleted.add(objectiveId);write({...local,deletedObjectiveIds:[...deleted],objectives:(local.objectives||[]).filter(o=>o.id!==objectiveId),plans:(local.plans||[]).filter(p=>p.objectiveId!==objectiveId)});}
  async function restoreBackend(){const local=read();if(!local.savedAt)return;try{await originalFetch('/api/plans/continuity/restore',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({deletedObjectiveIds:local.deletedObjectiveIds||[],objectives:local.objectives||[],plans:local.plans||[],planVersions:local.planVersions||[],planProposals:local.planProposals||[]})});}catch(error){console.warn('Plan continuity restore',error);}}
  window.fetch=async function(input,init){const response=await originalFetch(input,init);try{const url=typeof input==='string'?input:input?.url||'',method=String(init?.method||input?.method||'GET').toUpperCase();if(method==='GET'&&/\/api\/dashboard(?:\?|$)/.test(url)&&response.ok){const data=await response.clone().json(),body=JSON.stringify(overlay(data));return new Response(body,{status:response.status,statusText:response.statusText,headers:response.headers});}}catch(error){console.warn('Plan continuity overlay',error);}return response;};
  window.coachPlanContinuity={read,capture,markDeleted,restoreBackend,overlay};
  setTimeout(async()=>{await restoreBackend();try{if(typeof safeReload==='function')await safeReload();}catch{}},900);
})();
